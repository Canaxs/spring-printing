spring:
  datasource:
    url: jdbc:postresql://localhost:5432/print
    username: "root"
    password: "root"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect